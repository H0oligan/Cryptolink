/// <reference types="vite/client" />

// Injected by vite at build time from vite.config.ts. Single source of truth
// for the public CryptoLink release tag (single-decimal scheme: 1.0, 2.0, …).
declare const __CRYPTOLINK_VERSION__: string;
